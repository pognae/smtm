# smtm 운영 매뉴얼

버전 1.2.0 기준. 소스(`smtm/__main__.py`, Controller, TelegramController, Simulator, MassSimulator, StrategyFactory)를 보고 정리했다.

smtm은 암호화폐 자동매매 프로그램이다. 정해진 간격으로 다음을 반복한다.

1. Data Provider가 캔들(OHLCV)을 가져온다.
2. Strategy가 매수/매도/취소 요청을 만든다.
3. Trader가 주문한다.
4. Analyzer가 수익률과 그래프를 만든다.

초 단위 고빈도 매매용이 아니다. 실전은 1분 캔들, 루프 간격 60초로 검증된 구조다. 실계좌 주문은 손실이 날 수 있다. 처음에는 시뮬레이션과 텔레그램 데모 모드만 쓴다.

---

## 1. 설치

저장소에 `requirements.txt`는 없다. 의존성은 `setup.py`에 있다.

```bash
python -m pip install -e .
```

개발/테스트까지 필요하면 coverage를 추가한다.

```bash
python -m pip install -e ".[dev]"
```

주요 패키지: requests, pandas, numpy, matplotlib, mplfinance, pyjwt, python-dotenv, jupyter, psutil, scikit-learn.

Python 분류자는 3.6으로 적혀 있지만, 현재 scikit-learn/pandas는 그보다 높은 버전을 요구한다. 3.10 이상을 권장한다.

시뮬레이션은 업비트 캔들 시각을 한국 시간으로 다룬다. OS 타임존을 `Asia/Seoul`로 맞춘다.

Windows PowerShell:

```powershell
tzutil /s "Korea Standard Time"
```

Linux:

```bash
sudo timedatectl set-timezone Asia/Seoul
```

작업 디렉터리는 프로젝트 루트(`smtm.db`, `log/`, `output/`가 생기는 곳)로 둔다.

---

## 2. 실행 모드

```bash
python -m smtm --mode <0~5> [옵션]
```

인자를 생략하면 도움말만 출력하고 종료한다 (`--mode` 기본값 6).

| mode | 이름 | 실주문 |
|---:|---|---|
| 0 | 대화형 시뮬레이터 | 없음 |
| 1 | 단일 시뮬레이션 1회 실행 | 없음 |
| 2 | CLI 실전 매매 | 있음 |
| 3 | 텔레그램 챗봇 매매 | 있음. `--demo 1`이면 가상 체결 |
| 4 | 설정 파일 대량 시뮬레이션 | 없음 |
| 5 | 대량 시뮬레이션 설정 파일 생성 | 없음 |

공통 옵션:

| 옵션 | 의미 | 기본값 |
|---|---|---|
| `--budget` | 소프트웨어 예산(원) | 10000 |
| `--term` | **매매 루프 간격(초)**. 캔들 주기가 아니다 | 60 |
| `--strategy` | 전략 코드. `BNH` `SMA` `RSI` `SML` | BNH |
| `--currency` | `BTC` `ETH` `DOGE` `XRP` | BTC |
| `--trader` | mode 2 전용. `0` 업비트, `1` 빗썸 | 0 |
| `--from_dash_to` | 시뮬 기간 `YYMMDD.HHMMSS-YYMMDD.HHMMSS` | 201220.170000-201220.180000 |
| `--config` | mode 4 설정 JSON | 없음 |
| `--process` | mode 4 프로세스 수. `-1`이면 CPU 수 | -1 |
| `--offset` | mode 5 구간 길이(분) | 120 |
| `--title` | mode 5 제목 | SMA_2H_week |
| `--file` | mode 5 출력 파일 | `output/generated_config.json` |
| `--log` | `log/` 아래 로그 파일 이름 | smtm.log |
| `--demo` | mode 2·3. `1`이면 가상 체결 | 0 |
| `--slippage` | 시뮬 체결 불리 비율. 0이면 요청가 | 0 |
| `--max_loss` | 누적 손실 퍼센트 한도. 0이면 끔 | 0 |
| `--strategy_params` | 전략 파라미터 JSON 객체 | 없음 |
| `--token` | 텔레그램 봇 토큰. 없으면 환경변수 | 없음 |
| `--chatid` | 텔레그램 chat id | 없음 |

주의:

- `--strategy`는 `BNH` `SMA` `RSI` `SML`을 쓴다. 예전 숫자 `0` `1` `2` `3`도 같은 순서로 받는다.
- `--strategy_params`는 선택 JSON이다. 예: `{"short": 5, "mid": 20, "long": 40}`. 없으면 전략 기본값이다.
- `--slippage`는 시뮬레이션 체결 비율이다. `0.001`이면 매수는 0.1% 비싸게, 매도는 0.1% 싸게 체결된다. 기본값 `0`이면 요청가다.
- `--max_loss`는 누적 수익률이 이 퍼센트만큼 손실이면 신규 주문을 멈춘다. `10`이면 -10%에서 정지. `0`이면 끄다.
- mode 2의 `--demo 1`은 업비트 시세와 가상 체결이다. 실주문은 `--demo`를 빼야 한다.
- `--term`과 캔들 주기는 별개다. 캔들은 `smtm/config.py`의 `candle_interval = 60`(1분)으로 고정이다. 3분/5분/10분(180/300/600)은 코드에서만 바꿀 수 있다.
- 실전에서 `--term`을 60보다 짧게 두면 같은 1분 캔들을 여러 번 전략에 넣는다. SMA/RSI가 왜곡된다. 실전 `--term`은 60 이상으로 둔다.
- 시뮬레이션의 `--term`은 재생 속도다. `0.001`처럼 1초 미만이면 타이머 없이 바로 다음 캔들로 넘어가 빨라진다. 데이터 간격 자체가 바뀌지는 않는다.
- mode 3의 매매 간격은 `--term`이 아니라 항상 60초다. `--max_loss`는 mode 3에도 적용된다.

### 전략

| 코드 | 이름 | 동작 |
|---|---|---|
| BNH | Buy and Hold | 매 틱마다 남은 예산의 1/5를 종가로 매수하고 보유. 매도 없음 |
| SMA | SMA0-I | 단기 10, 중기 40, 장기 60 이평. 단기>중기>장기이면 매수, 역배열이면 매도. 변동성 필터 있음 |
| RSI | RSI | 14기간 RSI. 30 이하 매수, 70 이상 전량 매도 |
| SML | SMA with ML | SMA 배열에 최근 20개 종가의 선형회귀 기울기가 양수일 때만 매수. 매도 후 20캔들 대기 |

수수료는 전략 안에서 0.05%(`COMMISSION_RATIO = 0.0005`)로 가정한다. 거래소 실제 수수료와 다를 수 있다.

최소 주문금액 기본값은 BNH/SMA/SML 5,000원, RSI 100원이다. 업비트 최소 주문은 보통 5,000원이므로 예산은 그보다 충분히 크게 잡는다. BNH는 5분할이라 예산 25,000원 미만이면 주문이 바로 스킵될 수 있다.

SMA/SML은 장기 이평 60개가 쌓이기 전에는 주문하지 않는다. 짧은 시뮬레이션 구간에서는 거래가 0건일 수 있다.

---

## 3. 시뮬레이션 (mode 0, 1)

과거 캔들은 업비트 공개 API에서 받아 `smtm.db`에 캐시한다. API 키는 필요 없다. 지원 마켓은 KRW-BTC, KRW-ETH, KRW-DOGE, KRW-XRP뿐이다. 빗썸 과거 데이터 시뮬은 없다.

기간 형식은 `YYMMDD.HHMMSS`다. 연도는 2자리(20 = 2020). 끝 시각이 시작보다 커야 하고, 길이는 캔들 간격(기본 1분)의 배수여야 한다.

### 3.1 대화형 (mode 0)

```bash
python -m smtm --mode 0 --budget 500000 --from_dash_to 201220.170000-201220.180000 --term 0.1 --strategy BNH --currency BTC
```

프롬프트 `input command (h:help):`

| 입력 | 동작 |
|---|---|
| `h` | 도움말 |
| `i` | 기간, 간격, 예산, 전략, 화폐를 다시 입력하고 초기화 |
| `r` | 시뮬레이션 시작. 먼저 `i` 또는 생성자 초기화가 끝나 있어야 함 |
| `s` | 중지 |
| `t` | 종료 |
| `1` | 상태 |
| `2` | 수익률 |
| `3` | 체결 기록 |

`r` 전에 초기화가 없으면 `초기화가 필요합니다`가 나온다. 설정을 바꾸려면 `s` 후 `i`를 다시 한다.

### 3.2 단일 실행 (mode 1)

초기화 후 끝까지 돌리고 결과를 낸다.

```bash
python -m smtm --mode 1 --budget 500000 --from_dash_to 201220.080000-201221.080000 --term 0.001 --strategy SMA --currency ETH
```

긴 구간은 업비트에서 캔들을 나눠 받으므로 첫 실행이 느리다. 같은 구간을 다시 돌리면 `smtm.db`를 쓴다.

---

## 4. 실전 CLI (mode 2)

업비트 예시:

```bash
python -m smtm --mode 2 --budget 100000 --term 60 --strategy BNH --currency ETH
```

빗썸:

```bash
python -m smtm --mode 2 --budget 100000 --term 60 --strategy RSI --currency BTC --trader 1
```

프로젝트 루트 `.env`:

```
UPBIT_OPEN_API_ACCESS_KEY=
UPBIT_OPEN_API_SECRET_KEY=
UPBIT_OPEN_API_SERVER_URL=https://api.upbit.com

BITHUMB_API_ACCESS_KEY=
BITHUMB_API_SECRET_KEY=
BITHUMB_API_SERVER_URL=
```

키가 없으면 기본 더미 문자열이 들어가 주문이 실패한다. `.env`는 커밋하지 않는다.

시작 시 출력 예:

```
##### smtm is intialized #####
interval: 60.0, strategy: Buy and Hold , trader: Upbit
```

프롬프트 `명령어를 입력하세요. (h: 도움말):`

| 입력 | 동작 |
|---|---|
| `h` | 도움말 |
| `r` | 자동매매 시작. 상태가 `ready`일 때만 성공 |
| `s` | 중지. 미체결 취소 후 보고서 생성. 이후 상태는 다시 `ready` |
| `t` | 중지 후 프로세스 종료. Ctrl+C, SIGTERM도 동일 |
| `q` | 이어서 `1` 상태, `2` 수익률, `3` 체결 기록 |

운영 순서: 키 확인 → 작은 `--budget` → `r` → `q`로 상태/체결 확인 → `s` 또는 `t`.

실주문으로 `r` 또는 텔레그램 `시작`을 하면, 해당 마켓 미체결을 취소한 뒤 거래소 잔고를 읽는다. 전략이 쓰는 원화는 `min(예산, 주문 가능 원화)`이고 코인 수량은 계좌 수량을 따른다. 조회가 실패하면 상태가 `ready`가 되지 않아 매매가 시작되지 않는다. 데모와 시뮬레이션은 이 조회를 하지 않고 `--budget`만 쓴다.

프로세스를 다시 켜도 같은 조회를 하므로, 이전에 산 코인은 SMA/RSI/SML 수량에 반영된다. BNH는 현금만 맞추고 매도는 하지 않는다.

지원 화폐:

- 업비트: BTC, ETH, DOGE, XRP
- 빗썸: BTC, ETH

콘솔 로그 레벨은 WARNING(30)이다. 주문 상세는 `log/smtm.log`에 있다.

---

## 5. 텔레그램 (mode 3)

1. BotFather로 봇을 만들고 토큰을 받는다.
2. 봇에게 메시지를 보낸 뒤 `getUpdates`로 `chat.id`를 확인한다. 그룹이 아니라 본인과의 1:1 방을 쓴다. 다른 chat id 메시지는 무시한다.
3. `.env` 또는 인자로 넣는다.

```
TELEGRAM_BOT_TOKEN=123456:ABC...
TELEGRAM_CHAT_ID=123456789
```

데모(시세는 업비트 공개 API, 체결은 가상, 키 불필요):

```bash
python -m smtm --mode 3 --demo 1
```

실주문:

```bash
python -m smtm --mode 3
```

토큰을 인자로 줄 수도 있다. 셸 히스토리에 남으므로 `.env`를 권장한다.

```bash
python -m smtm --mode 3 --demo 1 --token <토큰> --chatid <chat id>
```

봇은 long polling(10초)이다. 웹훅이 아니다. 프로세스가 떠 있어야 명령이 먹는다.

채팅 버튼:

| 명령 | 동작 |
|---|---|
| `1` / `시작` | 설정 마법사 후 매매 시작 |
| `2` / `중지` | 중지하고 요약(자산, 수익률, 비교 수익률) 전송 |
| `3` / `상태` | ready / running 등 |
| `4` / `수익` | 기간 선택 후 수익률과 그래프 이미지 |
| `5` / `거래` | 체결 목록 |

`시작` 질문 순서:

1. 예산. 버튼은 50000, 100000, 500000, 1000000. 다른 정수도 입력 가능.
2. 화폐. BTC, ETH, DOGE, XRP.
3. 거래소. `1` 업비트, `2` 빗썸. 빗썸은 BTC/ETH만.
4. 전략. `0` BNH, `1` SMA, `2` RSI, `3` SML 또는 이름/코드.
5. `1` Yes면 시작. 간격은 60초 고정.

중간에 잘못된 값이면 마법사가 취소되고 처음부터 다시 `시작`해야 한다. 매매 중 예외가 나면 워커가 멈추고 경고 메시지를 보낸다.

서버에서 터미널을 끊어도 유지하려면(Linux):

```bash
nohup python -m smtm --mode 3 --demo 1 > nohup.out 2> nohup.err < /dev/null &
```

Windows는 작업 스케줄러나 `Start-Process`로 같은 역할을 한다. 데모로 하루 이상 확인한 뒤에만 `--demo`를 빼서 실주문을 켠다.

---

## 6. 대량 시뮬레이션 (mode 4, 5)

한 전략을 여러 기간에 멀티프로세스로 돌리고 수익률 평균/표준편차/최대/최소를 낸다.

### 6.1 설정 생성 (mode 5)

`--offset` 분 단위로 `from_dash_to` 구간을 자른다.

```bash
python -m smtm --mode 5 --budget 500000 --title SMA_6H_week --strategy SMA --currency ETH --from_dash_to 210804.000000-210811.000000 --offset 360 --file generated_config.json
```

기본 출력 경로 `output/generated_config.json`은 `MassSimulator` 인스턴스를 만들지 않고 저장하므로 `output/` 폴더가 없으면 실패할 수 있다. `--file`을 지정한다.

생성 JSON:

```json
{
  "title": "SMA_6H_week",
  "description": "write descriptionf here",
  "budget": 500000,
  "strategy": "SMA",
  "interval": 1e-06,
  "currency": "ETH",
  "period_list": [
    {"start": "2021-08-04T00:00:00", "end": "2021-08-04T06:00:00"}
  ]
}
```

`strategy`는 반드시 `BNH`/`SMA`/`RSI`/`SML`이다. `interval`은 시뮬 재생 속도(초)다. `description`은 생성기가 오타 문구를 넣으므로 실행 전에 고친다.

`example_data/sma0_simulation.json`의 전략은 `BNH`, `example_data/generated_config.json`은 `SMA`다. 숫자 `0`/`1`도 동작하지만 새 파일에는 코드를 쓴다. 선택 필드로 `strategy_params`, `slippage`, `max_loss`를 넣을 수 있다.

### 6.2 실행 (mode 4)

```bash
python -m smtm --mode 4 --config generated_config.json --process 4
```

`--config`가 비어 있으면 도움말 후 종료한다. `--process`가 기간 개수보다 크면 기간 개수로 줄인다.

결과:

- 화면: 수익률 평균, 편차, 최대, 최소
- `output/<title>.result` 텍스트 요약. 구간이 10개 넘으면 상위/하위 10개
- `output/<title>.jpg` 구간별 수익률 막대(빨간 선은 평균)
- 로그: `log/mass-simulation.log`, 프로세스별 `log/mass-simulation-<n>.log`

결과 파일의 인덱스는 `period_list` 순번이다. 최고 수익 구간을 그 인덱스로 찾는다.

---

## 7. Jupyter 컨트롤러

`JptController`는 노트북에서 실전(또는 거래소 API) 루프를 돌린다. 시뮬레이션 클래스가 아니다.

```python
from smtm import JptController

ctl = JptController()
ctl.initialize(interval=60, strategy="BNH", budget=100000, is_bithumb=False)
ctl.start()
ctl.get_state()
ctl.get_score()          # 수익률 출력 + 그래프 이미지
ctl.get_trading_record()
ctl.stop()               # 이후 다시 initialize 필요
```

`JptController.initialize()`의 `strategy` 기본값은 `"BNH"`다.

---

## 8. 산출물과 로그

| 경로 | 내용 |
|---|---|
| `log/smtm.log` | 회전 로그. 2MB × 백업 10개. 실전/시뮬 기본 |
| `log/mass-simulation*.log` | 대량 시뮬 |
| `smtm.db` | 업비트 과거 캔들 SQLite |
| `output/g*.jpg` | 실행 중 수익률 그래프 |
| `output/<tag>.txt` 등 | `Analyzer.create_report` 중지 시 보고서 |
| `output/<title>.result`, `.jpg` | 대량 시뮬 요약 |

실전 Operator는 약 5시간(`PERIODIC_RECORD_INTERVAL_SEC = 300 * 60`)마다 최근 360턴 그래프를 `output/`에 저장한다. 턴이 60초면 최근 6시간이다.

수익률 튜플 순서:

0. 시작 자산
1. 최종 자산
2. 누적 수익률
3. 같은 구간 가격 변화율(비교 수익률)
4. 그래프 경로
5. 기간
6. 구간 최고 수익률
7. 구간 최저 수익률
8. 시작/구간 시각

상태: 초기화 전 `None` → `ready` → `running` → 중지 중 `terminating` → `ready`. `running`이 아니면 시작과 수익률 조회가 거절된다.

---

## 9. 테스트

단위 테스트(거래소 호출 없음):

```bash
python -m unittest discover ./tests *test.py -v
```

통합 테스트는 실제 업비트/빗썸 API를 친다. 키와 네트워크가 필요하고, 일부는 주문이 나간다. 운영 서버에서 습관적으로 돌리지 않는다.

```bash
python -m unittest integration_tests
```

---

## 10. 운영 체크리스트

실주문 전:

- [ ] 타임존 KST
- [ ] `.env` 키와 URL. 출금 권한 없는 주문 전용 키
- [ ] `--strategy`가 BNH/SMA/RSI/SML
- [ ] `--term 60` (mode 2). mode 3는 코드상 60초 고정인지 확인
- [ ] `--budget`이 계좌 원화 이하이고, 최소 주문(5,000원)과 분할 매수를 감당
- [ ] 화폐가 거래소 지원 목록 안
- [ ] mode 3 데모로 시작/중지/수익 조회가 되는지 확인
- [ ] `log/smtm.log`에 키 값이 찍히지 않는지 확인

실행 중:

- [ ] `q` / 텔레그램 `상태`가 running
- [ ] 첫 캔들 이후 체결 또는 “금액 부족/조건 미충족” 로그가 정상인지
- [ ] SMA는 60분 이전에는 주문이 없는 것이 정상

중지:

- [ ] `s` 또는 텔레그램 `중지`로 미체결 취소와 보고서 생성
- [ ] 거래소 앱에서 잔고·미체결이 보고서와 같은지 대조
- [ ] 프로세스만 죽이지 말 것. 가능하면 `t`/중지 후 종료

---

## 11. 자주 막히는 지점

| 증상 | 원인 |
|---|---|
| `Invalid Strategy` | 지원하지 않는 전략 코드. `BNH` `SMA` `RSI` `SML` 또는 `0`~`3` |
| 실전 시작이 바로 실패하고 계좌 조회 안내가 나옴 | `.env` 키, URL, 네트워크. 조회 실패 시 주문하지 않음 |
| 누적 손실 후 상태가 `halted` | `--max_loss`에 도달했거나 주문/시세 오류가 5회 연속 |
| `Please check your telegram chat-bot token` | 토큰 없음 |
| 텔레그램에 반응이 없음 | chat id 불일치, 프로세스 다운, 폴링 실패 |
| 시뮬이 바로 끝나거나 데이터 없음 | 기간 형식 오류, 미래 시각, 업비트에 없는 구간 |
| `sec is not multiple of 1 min` | 기간이 1분 배수가 아님 |
| 거래 0건 | 예산이 최소 주문보다 작음, SMA 워밍업 60캔들 부족, 조건 미충족 |
| 실전인데 같은 가격이 연속 기록 | `--term`이 캔들(60초)보다 짧음 |
| mode 2에 `--demo 1`을 줬는데 실주문 | 현재는 데모 체결이다. 실주문은 `--demo`를 뺀다 |
| `output/generated_config.json` 실패 | `output/` 디렉터리 없음. `--file` 지정 |

---

## 12. 소스 기준 개선점

우선순위는 운영 사고와 결과 신뢰도 기준이다.

### 반영된 항목

1. 전략 코드 `BNH`/`SMA`/`RSI`/`SML`과 숫자 별칭 `0`~`3`을 함께 받는다. README, 예제 JSON, Jupyter 기본값은 코드 문자열이다.
2. `requirements.txt`, `requirements-dev.txt`가 있다.
3. mode 2 `--demo 1`은 `DemoTrader`다.
4. 실전 시작 시 해당 마켓 미체결을 취소하고 계좌 잔고를 맞춘다. 조회 실패 시 시작하지 않는다.
5. `--strategy_params`와 대량 시뮬 `strategy_params`로 이평·RSI·수수료를 덮어쓸 수 있다. 생략하면 기존 상수다.
6. `--slippage`와 대량 시뮬 `slippage`. 기본 0이라 기존 시뮬 결과와 같다.
7. `--max_loss`로 누적 손실 시 정지한다. 주문/시세 예외는 다음 턴을 유지하고, 5회 연속이면 `halted`가 된다.

### 남아 있는 것

1. **캔들 주기와 루프 주기가 분리돼 있다.** 실전 `--term`을 60보다 짧게 두면 같은 1분 캔들이 반복 유입된다.
2. **텔레그램 실전 간격은 60초 고정**이다. `--term`은 mode 3에 적용되지 않는다.
3. **BNH는 매도가 없다.** 계좌의 코인 수량을 읽어도 팔지 않는다.
4. **슬리피지는 비율만 반영**한다. 호가 공백, 부분 체결, 체결 지연은 없다.
5. **일 손실·주문 금액 상한은 없다.** 한도는 누적 수익률과 연속 실패뿐이다.
6. **비교 수익률은 단순 가격 변화**다. 수수료를 뺀 보유 성과가 아니다.
7. **시뮬레이션 데이터는 업비트 캔들만** 저장한다.
8. **Python 3.6 분류자, `is_intialized` 오타, 콘솔 로그 WARNING**은 그대로다.
9. **단일 심볼·단일 전략**이다. 여러 코인을 동시에 돌리면 프로세스마다 예산을 나눠야 한다.
10. **대량 시뮬 결과 인덱스는 기간 순번**이다. 날짜는 `period_list`에서 찾는다.
11. **통합 테스트는 실거래소에 붙는다.** 운영 서버에서 돌리지 않는다.
12. **키 권한, 출금 API 비활성, 허용 IP**는 거래소 설정으로 유지한다. 봇 토큰이 유출되면 BotFather에서 재발급한다.
